"use client";
import { useEffect,useState } from "react";import { useRouter } from "next/navigation";import { HeartPulse } from "lucide-react";import { useAuth } from "@/context/AuthContext";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Select } from "@/components/ui/select";
export default function Page(){const {user,supabase,loading}=useAuth();const router=useRouter();const [saving,setSaving]=useState(false),[error,setError]=useState("");const [form,setForm]=useState({specialty:"",state:"CE",has_private_practice:false,does_shifts:true,has_company:false,track_personal_finance:true,track_investments:true,main_goal:"Organizar minha renda médica"});useEffect(()=>{if(!loading&&!user)router.replace('/login')},[loading,user,router]);async function save(e:any){e.preventDefault();if(!user)return;setSaving(true);setError("");const {error}=await supabase.from('medical_profiles').upsert({user_id:user.id,...form,onboarding_completed:true,updated_at:new Date().toISOString()});if(error){setError(error.message);setSaving(false);return}router.replace('/medicos/dashboard')}
return <main className="min-h-screen bg-[#eef5f9] p-4 md:p-8"><div className="mx-auto max-w-3xl rounded-[2rem] bg-white p-6 shadow-xl md:p-10"><span className="grid size-14 place-items-center rounded-2xl bg-[#0f4c81] text-white"><HeartPulse className="size-6"/></span><p className="mt-6 text-xs font-black uppercase tracking-[.2em] text-[#0f4c81]">Primeira configuração</p><h1 className="mt-2 text-3xl font-black">Vamos adaptar o Equity One à sua rotina médica.</h1><p className="mt-3 text-sm leading-6 text-slate-500">Leva menos de dois minutos. Essas respostas organizam os atalhos e indicadores iniciais.</p><form onSubmit={save} className="mt-8 grid gap-5 md:grid-cols-2"><label><span className="mb-2 block text-xs font-black uppercase text-slate-500">Especialidade</span><Input required value={form.specialty} onChange={e=>setForm({...form,specialty:e.target.value})} placeholder="Ex.: Cardiologia"/></label><label><span className="mb-2 block text-xs font-black uppercase text-slate-500">Estado</span><Select value={form.state} onChange={e=>setForm({...form,state:e.target.value})}>{['AC','AL','AP','AM','BA','CE','DF','ES','GO','MA','MT','MS','MG','PA','PB','PR','PE','PI','RJ','RN','RS','RO','RR','SC','SP','SE','TO'].map(x=><option key={x}>{x}</option>)}</Select></label>{[["does_shifts","Realizo plantões"],["has_private_practice","Atendo particular"],["has_company","Possuo CNPJ/PJ"],["track_personal_finance","Quero acompanhar finanças pessoais"],["track_investments","Quero acompanhar investimentos"]].map(([k,l])=><label key={k} className="flex items-center gap-3 rounded-2xl border border-slate-200 p-4 text-sm font-bold"><input type="checkbox" checked={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.checked})}/>{l}</label>)}<label className="md:col-span-2"><span className="mb-2 block text-xs font-black uppercase text-slate-500">Objetivo principal</span><Select value={form.main_goal} onChange={e=>setForm({...form,main_goal:e.target.value})}>{['Organizar minha renda médica','Controlar plantões e recebimentos','Aumentar meu valor por hora','Reduzir dependência de plantões','Crescer consultório particular','Construir patrimônio'].map(x=><option key={x}>{x}</option>)}</Select></label>{error&&<div className="md:col-span-2 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</div>}<Button disabled={saving} className="h-12 bg-[#0f4c81] md:col-span-2">{saving?'Salvando...':'Entrar no meu dashboard'}</Button></form></div></main>}
