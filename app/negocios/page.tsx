import Link from "next/link";import { BarChart3,Check,FileText,ReceiptText,TrendingUp } from "lucide-react";import { Button } from "@/components/ui/button";
import { EquityOneLogo } from "@/components/EquityOneLogo";import { ProductCheckout } from "@/components/commerce/ProductCheckout";import { getPlatformPricingSettings } from "@/lib/pricing";
export default async function Page(){const pricing=await getPlatformPricingSettings();return <main className="min-h-screen bg-[#f4f3ef]"><header className="border-b border-black/5 bg-white/70"><div className="mx-auto flex h-24 max-w-7xl items-center justify-between px-4 md:px-8"><Link href="/" className="flex items-center gap-3"><EquityOneLogo className="h-16 md:h-20 w-auto"/><p className="hidden text-[10px] font-black uppercase tracking-[.16em] text-slate-500 sm:block">Negócios</p></Link><Link href="/login"><Button variant="outline">Já sou cliente</Button></Link></div></header><section className="mx-auto grid max-w-7xl gap-12 px-4 py-20 md:px-8 lg:grid-cols-[1.05fr_.95fr] lg:items-center"><div><p className="text-xs font-black uppercase tracking-[.2em] text-[#9a762b]">Equity One Negócios</p><h1 className="mt-4 text-5xl font-black tracking-[-.04em]">Caixa saudável começa com visibilidade.</h1><p className="mt-6 text-lg leading-8 text-slate-600">Centralize contas a pagar e receber, custos, recorrências e resultado para saber o que a empresa pode fazer antes de comprometer o caixa.</p><div className="mt-8 grid gap-3 sm:grid-cols-2">{["Fluxo de caixa e projeção","Contas a pagar e receber","Centros de custo","Relatórios e permissões"].map(x=><div key={x} className="flex gap-3 rounded-2xl bg-white p-4 text-sm font-bold"><Check className="size-4 text-emerald-600"/>{x}</div>)}</div></div><ProductCheckout plan="BUSINESS" priceCents={pricing.business_price_cents} enabled={pricing.business_checkout_enabled} label="Começar minha gestão empresarial"/></section><section className="mx-auto max-w-7xl px-4 pb-20 md:px-8"><div className="grid gap-4 md:grid-cols-4">{[[TrendingUp,"Fluxo de caixa","Acompanhe entradas, saídas e tendência."],[ReceiptText,"Compromissos","Controle pagar e receber sem planilhas."],[BarChart3,"Indicadores","Visualize resultado e evolução."],[FileText,"Relatórios","Transforme movimentações em gestão."]].map(([I,t,d]:any)=><div key={t} className="rounded-3xl bg-white p-6"><I className="size-5 text-[#9a762b]"/><h3 className="mt-4 font-black">{t}</h3><p className="mt-2 text-sm text-slate-500">{d}</p></div>)}</div></section></main>}
