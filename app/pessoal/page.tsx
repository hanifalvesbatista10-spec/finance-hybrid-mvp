import Link from "next/link";import { BellRing,CalendarDays,Check,Target,UserRound,WalletCards } from "lucide-react";import { Button } from "@/components/ui/button";
import { EquityOneLogo } from "@/components/EquityOneLogo";import { ProductCheckout } from "@/components/commerce/ProductCheckout";import { getPlatformPricingSettings } from "@/lib/pricing";
export default async function Page(){const pricing=await getPlatformPricingSettings();return <main className="min-h-screen bg-[#f4f3ef]"><Top icon={UserRound} label="Pessoal"/><section className="mx-auto grid max-w-7xl gap-12 px-4 py-20 md:px-8 lg:grid-cols-[1.05fr_.95fr] lg:items-center"><div><p className="text-xs font-black uppercase tracking-[.2em] text-[#9a762b]">Equity One Pessoal</p><h1 className="mt-4 text-5xl font-black tracking-[-.04em]">Saiba quanto pode gastar sem descobrir tarde demais.</h1><p className="mt-6 text-lg leading-8 text-slate-600">Organize contas, cartões, metas e despesas recorrentes em um dashboard que mostra o que precisa da sua atenção hoje.</p><div className="mt-8 grid gap-3 sm:grid-cols-2">{["Saldo e resultado do mês","Contas próximas do vencimento","Orçamento por categoria","Metas e calendário financeiro"].map(x=><div key={x} className="flex gap-3 rounded-2xl bg-white p-4 text-sm font-bold"><Check className="size-4 text-emerald-600"/>{x}</div>)}</div></div><ProductCheckout plan="PERSONAL" priceCents={pricing.personal_price_cents} enabled={pricing.personal_checkout_enabled} label="Começar minha gestão pessoal"/></section><section className="mx-auto max-w-7xl px-4 pb-20 md:px-8"><div className="grid gap-4 md:grid-cols-4">{[[WalletCards,"Controle diário","Veja entradas, despesas e saldo."],[BellRing,"Alertas","Antecipe contas e vencimentos."],[Target,"Metas","Acompanhe economia e objetivos."],[CalendarDays,"Calendário","Entenda o mês por dia."]].map(([I,t,d]:any)=><div key={t} className="rounded-3xl bg-white p-6"><I className="size-5 text-[#9a762b]"/><h3 className="mt-4 font-black">{t}</h3><p className="mt-2 text-sm text-slate-500">{d}</p></div>)}</div></section></main>}
function Top({label}:{icon:any;label:string}){return <header className="border-b border-black/5 bg-white/70"><div className="mx-auto flex h-24 max-w-7xl items-center justify-between px-4 md:px-8"><Link href="/" className="flex items-center gap-3"><EquityOneLogo className="h-16 md:h-20 w-auto"/><p className="hidden text-[10px] font-black uppercase tracking-[.16em] text-slate-500 sm:block">{label}</p></Link><Link href="/login"><Button variant="outline">Já sou cliente</Button></Link></div></header>}
